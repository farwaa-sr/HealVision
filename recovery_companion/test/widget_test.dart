// Placeholder test. The default counter test from `flutter create` doesn't
// apply to this app; real widget tests can be added per feature later.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('smoke: test harness runs', () {
    expect(1 + 1, 2);
  });
}
